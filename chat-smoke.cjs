const {app,BrowserWindow,ipcMain}=require('electron');const fs=require('fs'),path=require('path');app.setPath('userData',path.join(__dirname,'.qa','chat-profile'));require('./main.js');let calls=0;ipcMain.removeHandler('chat');ipcMain.handle('chat',async(_,text)=>{calls++;await new Promise(r=>setTimeout(r,700));return {chat:[{role:'user',text},{role:'assistant',text:'**A clear direction**\n\nGive each part room to breathe.\n\n```js\nconst beautiful = true;\n```\n\n<img src=x onerror=alert(1)>'}]}});app.whenReady().then(async()=>{await new Promise(r=>setTimeout(r,2200));let win=BrowserWindow.getAllWindows()[0];try{win.unmaximize();win.setContentSize(1000,720);const result=await win.webContents.executeJavaScript(`(async()=>{const check=(b,m)=>{if(!b)throw Error(m)};check(document.body.dataset.page==='Sparky','Initial route');document.querySelector('[data-prompt]').click();const draft=document.querySelector('#chat-input').value;check(draft.length>10,'Suggestion');document.querySelector('nav [data-page=Tasks]').click();document.querySelector('nav [data-page=Sparky]').click();check(document.querySelector('#chat-input').value===draft,'Draft retained');document.querySelector('#sidebar-toggle').click();check(document.body.classList.contains('sidebar-collapsed'),'Collapse');document.querySelector('#sidebar-toggle').click();const box=document.querySelector('#chat-input').getBoundingClientRect();check(box.bottom<innerHeight,'Composer visible');check(document.documentElement.scrollWidth<=innerWidth,'Overflow');document.querySelector('#chat-form').requestSubmit();document.querySelector('#chat-form').requestSubmit();check(document.querySelector('#send').disabled,'Busy');await new Promise(r=>setTimeout(r,900));check(document.querySelectorAll('.message').length===2,'Reply');check(document.querySelector('.message strong'),'Bold');check(document.querySelector('.message code'),'Code');check(!document.querySelector('.message img'),'Escaped HTML');check(document.querySelector('#chat-input').value==='','Draft cleared');check(document.querySelector('#send').disabled,'Empty send remains disabled');return {initialSparky:true,draftPreserved:true,suggestions:true,sidebar:true,narrowComposer:true,formattedReply:true,htmlEscaped:true};})()`);result.chatCalls=calls;if(calls!==1)throw Error('Duplicate send');fs.writeFileSync(path.join(__dirname,'.qa','chat-results.json'),JSON.stringify(result,null,2));app.exit(0)}catch(e){fs.writeFileSync(path.join(__dirname,'.qa','chat-results.json'),JSON.stringify({error:e.stack}));app.exit(1)}});

